# Copy this file to .env and fill in your Supabase project's values
# (Settings → API in your Supabase dashboard). Without these, the app
# runs fully offline/local and the sync UI stays hidden.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
